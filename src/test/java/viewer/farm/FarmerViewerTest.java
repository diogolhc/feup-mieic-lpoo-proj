package viewer.farm;

import gui.GUI;
import gui.drawer.entity.FarmerDrawer;
import model.Position;
import model.farm.Farmer;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;

public class FarmerViewerTest {
    /*
    private Farmer farmer;
    private FarmerViewer viewer;
    private GUI gui;

    @BeforeEach
    void setUp() {
        farmer = new Farmer(new Position(4, 8));
        viewer = new FarmerViewer();
        gui = Mockito.mock(GUI.class);
    }

    @Test
    void draw() {


        viewer.draw(farmer, gui);

        Mockito.clearInvocations(gui);
        FarmerDrawer farmerDrawer = new FarmerDrawer(gui);
        farmerDrawer.draw(new Position(4, 8));


        Mockito.framework().
        //viewer.draw();
        //Mockito.verify(gui, Mockito.times(1)).drawHero(hero.getPosition());
    }
    */


}
